
# This file was generated by the Tkinter Designer by Parth Jadhav
# https://github.com/ParthJadhav/Tkinter-Designer


from pathlib import Path

# from tkinter import *
# Explicit imports to satisfy Flake8
from tkinter import Tk, Canvas, Entry, Text, Button, PhotoImage

import subprocess
import sys

OUTPUT_PATH = Path(__file__).parent
ASSETS_PATH = OUTPUT_PATH / Path(r"G:/My Drive/Coding\DigitalQuizApp/quiz_gui\build/assets/frame0")

# --- FUNCTIONS ---
def relative_to_assets(path: str) -> Path:
    return ASSETS_PATH / Path(path)

# generating a function to make the start button work
def start_quiz(event):
    window.destroy()  # closing welcome screen
    python_executable = sys.executable  # finding the path
    subprocess.Popen([python_executable, "quiz_gui/build/gui1.py"])

window = Tk()

window.geometry("1440x1024")
window.configure(bg = "#3D3C3C")

# --- UI ---
canvas = Canvas(
    window,
    bg = "#3D3C3C",
    height = 1024,
    width = 1440,
    bd = 0,
    highlightthickness = 0,
    relief = "ridge"
)

canvas.place(x = 0, y = 0)
canvas.create_text(
    720.0,
    362.0,
    anchor="n",
    text="Digital Learning Quiz",
    fill="#F0F0F0",
    font=("Consolas", 64 * -1)
)

canvas.create_text(
    720.0,
    219.0,
    anchor="n",
    text="🧠",
    fill="#FFFFFF",
    font=("Consolas", 64 * -1)
)

# --- FUNCTIONAL BUTTONS --- 
start_button_rect = canvas.create_rectangle(
    520.0,
    639.0,
    920.0,
    759.0,
    fill="#746FF4",
    outline=""
)
canvas.tag_bind(start_button_rect, "<Button-1>", start_quiz)

start_button_text = canvas.create_text(
    720.0,
    699.0,
    anchor="center",
    text="Start Quiz",
    fill="#FFFFFF",
    font=("Consolas", 40 * -1)
)
canvas.tag_bind(start_button_text, "<Button-1>", start_quiz)

window.resizable(True, True)
window.mainloop()
